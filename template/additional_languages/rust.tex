\lstdefinelanguage{rust}{
    keywords={
        _,
        as,
        async,
        async,
        await,
        await,
        break,
        const,
        continue,
        crate,
        dyn,
        dyn,
        else,
        enum,
        extern,
        false,
        fn,
        for,
        if,
        impl,
        in,
        let,
        loop,
        match,
        mod,
        move,
        mut,
        pub,
        ref,
        return,
        static,
        super,
        true,
        type,
        unsafe,
        where,
        while
    },
    ndkeywords={
        enum,
        self,
        Self,
        struct,
        trait,
        use
    },
    sensitive=true,
    comment=[l]{//},
    morecomment=[s]{/*}{*/},
    delim=[s][{\itshape\color[rgb]{0,0,0.75}}]{\#[}{]},
    morestring=[b]"
}
