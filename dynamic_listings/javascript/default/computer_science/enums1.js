if (car.fuel_type === 1) {
    // Gasoline
    gasolinepump.use();
} else if (car.fuel_type === 2) {
    // Diesel
    dieselpump.use();
} else if (car.fuel_type === 3) {
    // LPG
    lpgpump.use();
}
