function count_backwards(n){
    // Condition for the loop
    while (n != 0){
        // The function body
        console.log(n);
        // We update the condition to count down
        n = n - 1;
    }
}
