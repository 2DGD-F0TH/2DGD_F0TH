local function count_backwards(n)
    -- Condition for the loop
    while (n ~= 0) do
        -- The function body
        print(n)
        -- We update the condition to count down
        n = n - 1
    end
end
