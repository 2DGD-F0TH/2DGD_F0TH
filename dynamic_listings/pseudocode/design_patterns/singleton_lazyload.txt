class LazySingleton {

    static LazySingleton instance = null;

    LazySingleton() {}

    Singleton static function getInstance() {
        // Multi-threading: manage race conditions
        // ----- Critical region start -----
        if (instance == null) {
            instance = new Singleton();
        }
        // ----- Critical region end -----

        return instance;
    }
}
