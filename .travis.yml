language: generic
os: xenial

before_install:
    - sudo apt-get install -y pandoc
    - sudo apt-get install -y texlive-full

script:
    - make

deploy:
    provider: releases
    file: "Book.pdf"
    draft: true
    skip_cleanup: true
    on:
        branch: master
